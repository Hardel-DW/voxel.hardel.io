export function EnchantmentConditionalEffect() {
    return (
        <div>
            {/* Conditional effect values */}
            {/*                        <div className="grid gap-4">
                            {Array.isArray(value) &&
                                isConditionalEnchantmentValueEffectKey(key) &&
                                value.map((item, index) => (
                                    <ToolConditionalEffect key={index.toString()} effect={item}>
                                        <ToolEnchantmentValueEffect effect={item.effect} />
                                    </ToolConditionalEffect>
                                ))}

                            {Array.isArray(value) &&
                                typedKey === "minecraft:attributes" &&
                                value.map((item) => (
                                    <div className="pt-4">
                                        <ToolAttribute key={item.id} effect={item} />
                                    </div>
                                ))}
                        </div>*/}
        </div>
    );
}
